# Bradán v4

Stock research and DCF valuation tool.

## Setup

```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Run

```bash
uvicorn app.main:app --reload
```

## Environment

Copy `.env.example` to `.env` and fill in your values.
